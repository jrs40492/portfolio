name: Build

on:
  push:
    branches:
    - master

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      uses: actions/checkout@v3
      - name: 'Build:dockerimage'
        uses: docker/build-push-action@v3
        with:
          registry: ghcr.io
          username: "jrs40492"
          password: ${{ secrets.PAT }}
          repository: jrs40492/portfolio
          tags: latest